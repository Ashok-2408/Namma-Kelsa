<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <application
        android:name=".NammaKelsaApp"
        android:allowBackup="true"
        android:icon="@drawable/app_logo_premium"
        android:roundIcon="@drawable/app_logo_premium"
        android:label="Namma-Kelsa"
        android:supportsRtl="true"
        android:theme="@style/Theme.NammaKelsa">
        <activity android:name=".features.splash.SplashActivity" android:exported="true" android:theme="@style/Theme.NammaKelsa">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".features.onboarding.OnboardingActivity" android:theme="@style/Theme.NammaKelsa"/>
        <activity android:name=".features.home.MainActivity" android:theme="@style/Theme.NammaKelsa"/>
        <service android:name=".core.firebase.FCMService" android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>
</manifest>
